import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'taxi-course-new',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
